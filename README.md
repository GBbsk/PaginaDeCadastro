Um cadastro com Js e boostrap usando MVC.

Atualizações futuras:
1. Banco de dados + backend real, o cadastro só mostra um alert. Precisa salvar de verdade com Node + Express + PostgreSQL, e o authService.js consumindo essas rotas.
2. Autenticação com JWT
3. Segurança básica --> 
bcrypt → nunca salvar senha pura no banco
rate limiting → bloqueia tentativas de força bruta no login
privar dados presentes nos metodos!